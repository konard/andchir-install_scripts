# API Key for authentication (optional)
# If not set, API key authentication is disabled
# Generate a secure random key, for example:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your_secure_api_key_here

# Directory containing scripts (optional)
# SCRIPTS_DIR=/path/to/scripts

# Directory containing data files (optional)
# DATA_DIR=/path/to/data

# Base URL for downloading scripts (optional)
# SCRIPTS_BASE_URL=https://raw.githubusercontent.com/andchir/install_scripts/refs/heads/main/scripts

# ========================================
# Protection Settings (Rate Limiting)
# ========================================

# Enable/disable protection mode (default: true)
# When enabled, the API tracks IP addresses and blocks those that make too many requests
PROTECTION_ENABLED=true

# Maximum number of requests allowed per time window (default: 10)
# If an IP exceeds this limit, it will be temporarily blocked
RATE_LIMIT_MAX_REQUESTS=10

# Time window in seconds for rate limiting (default: 60 = 1 minute)
# Requests are counted within this time window
RATE_LIMIT_TIME_WINDOW=60

# Path to the SQLite database for rate limiting (default: rate_limiter.db in api directory)
# RATE_LIMITER_DB_PATH=/path/to/rate_limiter.db
